# Démarrer un docker compose en arrière plan
docker compose up -d

# Supprimer les containers, les networks, pas les volumes
docker compose down

# Arrêter tous les containers du compose
docker compose stop

# Démarrer tous les containers du compose
docker compose start

# Redémarrer tous les containers du compose
docker compose restart
